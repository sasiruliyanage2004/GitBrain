import React from 'react';
import ReactDOM from 'react-dom/client';
import GitBrainApp from './SynapseApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GitBrainApp />
  </React.StrictMode>
);
